const authUrl = import.meta.env.VITE_BACK_AUTH_URL;

export const facebookAuthUrl = `${authUrl}/facebook`;
export const twitterAuthUrl = `${authUrl}/twitter`;
export const googleAuthUrl = `${authUrl}/google`;
